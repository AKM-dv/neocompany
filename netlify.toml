[build]
  # Static files only — no build step; repo root is the Vite `dist` output
  command = "true"
  publish = "."

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
